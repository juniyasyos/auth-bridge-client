Third-Party Licenses

This file should list all third-party dependencies and their licenses used by this project.

How to generate a list (example using Composer):

1. Install the `composer/composer` or use the `composer` CLI available in your environment.
2. Run:

```
composer licenses
```

Or list installed packages and inspect their licenses:

```
composer show -i --format=json > composer-installed.json
```

Populate this file with package names and their license declarations.

NOTE: This is a placeholder — update with the actual third-party license information.
